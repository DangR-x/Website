<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : mapper接口的完全限定名 -->
<mapper namespace="xiongjunmiao.top.Website.mapper.AdminMapper">
    <resultMap id="BaseResultMap" type="xiongjunmiao.top.Website.domain.Admin">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="icon" jdbcType="VARCHAR" property="icon" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="nick_name" jdbcType="VARCHAR" property="nickName" />
        <result column="note" jdbcType="VARCHAR" property="note" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="login_time" jdbcType="TIMESTAMP" property="loginTime" />
        <result column="status" jdbcType="INTEGER" property="status" />
    </resultMap>
<!---->
    <insert id="insert" parameterType="admin" keyProperty="id" useGeneratedKeys="true">
        insert into t_admin (
        username, password, icon,
        email, nick_name, note,
        create_time, login_time, status) value
        (
        #{username},#{password},#{icon},
        #{email},#{nickName},#{note},
        #{createTime},#{loginTime},#{status})
    </insert>
    <delete id="deleteById" parameterType="long">
        delete * from t_admin where id=#{id}
    </delete>
    <!--   -->
    <update id="updateById" parameterType="admin" >
        update t_admin a set
         a.username=#{username},
         a.password=#{password},
         a.icon=#{icon},
         a.email=#{email},
        a.nick_name=#{nickName},
        a.note=#{note},
        create_time = #{createTime},
        login_time = #{loginTime},
        a.status=#{status}
        where a.id=#{id}
    </update>
    <select id="selectById" resultMap="BaseResultMap">
        select * from t_admin where id=#{id}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select * from t_admin
    </select>
</mapper>